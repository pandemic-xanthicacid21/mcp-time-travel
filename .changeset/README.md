# Changesets

This directory stores release metadata for `mcp-time-travel`.

Run `npm run changeset` in feature or fix branches when a pull request changes shipped package behavior. Each file records the intended semver bump and a short summary that will be used for the release PR and changelog.

Docs-only, workflow-only, and test-only changes do not need a changeset.
